# Copy to .env and adjust per machine. Everything here is optional — config/config.yaml
# provides defaults that work out of the box. .env is gitignored (machine-specific).

# Override the auto-commit toggle without editing config.yaml (true/false).
# AUTO_COMMIT=true

# Override where the pipeline treats the app root as (App\). Defaults to two directories
# up from src/okeef/config.py; repo_root and bundle_root (Knowledgebase\) are derived from
# it as its parent and parent/Knowledgebase respectively. Only needed if you move App\
# relative to the rest of the repo.
# OKEEF_APP_ROOT=D:\OKEEF\App

# The Open WebUI Knowledge collection ID that openwebui_sync.py pushes concept docs
# into. This is per-machine (each Open WebUI instance generates its own UUID when you
# create the "OKEEF Bundle" collection during setup), so it belongs here, not in the
# shared config/config.yaml. See README.md for how to create the collection and get
# this value.
# OPENWEBUI_KNOWLEDGE_ID=

# Auto-creates the Open WebUI admin account headlessly on first startup (no browser
# signup needed), AND is reused by openwebui_sync.py to sign in for every sync (it
# gets a fresh session token per sync rather than depending on a generated API key --
# API keys were found not to reliably survive an Open WebUI restart on the installed
# version). Keep these set permanently, not just for first-run.
# WEBUI_ADMIN_EMAIL=
# WEBUI_ADMIN_PASSWORD=
# WEBUI_ADMIN_NAME=
